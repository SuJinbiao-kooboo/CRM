你好，可以帮我开发一个java 8 ，使用easyExcel 的方法，入参是两个java实体 list, 第一个入参是List<CrmOffer>，上面每一个字段带有ExcelProperties 注解，然后你：
1. 帮我读取一个指定地址的excel，这个地址来自字典crm_email_template_dict的key=excel_addr,复制副本，然后修改副本从第二行开始写入我的数据
2. 副本写入的数据第二行是我的表头，接下来部分是我的list的数据的值，当空的时候显示空字符串。
excel的表格帮我样式整理的好看一些，之后保留这个文件作为附件
3. 接下来使用我方法的第二个list，第二个list类型是字符串，里面每一个元素都是用逗号拼接了一些邮箱，接下来你遍历这写list，每一个元素都拆解里面的邮箱进行发送邮件，每一个元素的多个邮件要一起发送，不同元素的作为单独批次发送，邮件的内容如下：
3.1 一段富文本作为邮件的签名，可以在邮箱内友好展示渲染。签名来自字典crm_email_template_dict的key=email_sign
3.2 指定邮件的标题,这个标题来自字典crm_email_template_dict的key=email_title
3.3 指定邮件的正文内容,这个正文内容来自字典crm_email_template_dict的key=email_body
3.4 把刚刚附件的内容以表格的形式再次体现，完完整整的作为邮件正文后面的内容
3.5 把刚刚的附件作为邮件的附件进行发送
3.6 进行发送每发送一个都要打印发送结果日志，如果发送失败进行3次重试，如果还是失败则也不影响后续邮件的发送；
3.7 全部发送完成之后把数据记录下来作为一个集合返回每一个邮件的发送结果，整个结果信息显示出来在弹窗上，对成功和失败的邮件进行汇总。共用户复制，失败的要提示每一个失败邮件的失败原因。
3.8 读取crm_email_template_dict字典的key=email_account 作为邮箱账号
    读取crm_email_template_dict字典的key=email_password 作为邮箱密码
    读取crm_email_template_dict字典的key=email_smtp 作为邮件发送服务器地址
    读取crm_email_template_dict字典的key=email_smtp_port 作为smtp端口