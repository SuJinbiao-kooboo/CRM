<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crm.system.mapper.SysDictMapper">
    
    <sql id="selectSysDictVo">
        select id, dict_name, dict_key, description, status, create_by, create_time, update_by, update_time, remark from sys_dict
    </sql>

    <select id="selectSysDictList" parameterType="SysDict" resultType="SysDict">
        <include refid="selectSysDictVo"/>
        <where>  
            <if test="dictName != null  and dictName != ''"> and dict_name like concat('%', #{dictName}, '%')</if>
            <if test="dictKey != null  and dictKey != ''"> and dict_key like concat('%', #{dictKey}, '%')</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
        order by create_time desc
    </select>
    
    <select id="selectSysDictById" parameterType="Long" resultType="SysDict">
        <include refid="selectSysDictVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertSysDict" parameterType="SysDict" useGeneratedKeys="true" keyProperty="id">
        insert into sys_dict
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="dictName != null">dict_name,</if>
            <if test="dictKey != null">dict_key,</if>
            <if test="description != null">description,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="dictName != null">#{dictName},</if>
            <if test="dictKey != null">#{dictKey},</if>
            <if test="description != null">#{description},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateSysDict" parameterType="SysDict">
        update sys_dict
        <trim prefix="SET" suffixOverrides=",">
            <if test="dictName != null">dict_name = #{dictName},</if>
            <if test="dictKey != null">dict_key = #{dictKey},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteSysDictById" parameterType="Long">
        delete from sys_dict where id = #{id}
    </delete>

    <delete id="deleteSysDictByIds" parameterType="String">
        delete from sys_dict where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>